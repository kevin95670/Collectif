<form class="d-inline-block align-bottom card-link" method="post" action="{{ path('leave_event', {'id': event_id, 'participant': participant}) }}">
    <button class="btn btn-danger mt-3">Quitter cet évènement</button>
</form>
