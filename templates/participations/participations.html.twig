{% extends 'base.html.twig' %}

{% block title %}Participations{% endblock %}

{% block body_id 'participations' %}

{% block body %}

    <div class="container-fluid js-filter">
        <div class="row">
            <div class="col-sm-3">
            {% block aside %}
                {% include 'events/_filters.html.twig' %}
            {% endblock %}
            </div>
            <div class="col-sm-9">
                <div class="js-filter-content row">
                    {% include 'events/_events.html.twig' %}
                </div>
                <div class="navigation js-filter-pagination">
                    {% include 'events/_pagination.html.twig' %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
